# Device Recipes

Device Recipes are portable text-based protocol definitions.

They support BLE, CAN, RS485, UART, I2C, SPI, GPIO, HTTP, MQTT, Modbus, IR, OneWire, and custom transports.

Recipes separate public protocol knowledge from private local overlays and secrets.
