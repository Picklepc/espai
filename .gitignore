# Runtime logs
hub_start.log
*.log

# Runtime / secrets
.env
*.private.yaml
*.private.json
local.yaml
secrets.yaml
firmware/seed/secrets.ini

# Workspace runtime dirs (tracked by .gitkeep)
data/*.db
data/*.log
logs/
runtime/
backups/
captures/private/

# User project data — created at runtime, not tracked in repo
projects/

# Custom (local-only) registry items — not published to GitHub.
# Mark an item as custom by placing it in the custom/ subfolder:
#   recipes/custom/my-recipe/   workers/custom/my-worker/   cards/custom/my-card/
recipes/custom/
workers/custom/
cards/custom/

# Firmware catalog binaries (metadata JSONs are tracked, binaries are not)
firmware-catalog/**/*.bin

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.pytest_cache/

# PlatformIO
.pio/
firmware/**/.pio/

# Node / frontend build artifacts
node_modules/
dist/
build/

# Editor
.vscode/settings.json
*.swp
*~

