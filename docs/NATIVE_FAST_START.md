# Native Fast Start

Commands:

```bash
ESPAI init
ESPAI doctor
ESPAI serve
```

Dependency installation should be explicit:

```bash
ESPAI install-deps --python --platformio
```

Do not silently install dependencies.
