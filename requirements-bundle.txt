# ESPAI Hub — locked requirements for reproducible PyInstaller bundles
#
# Generate a fresh lock:
#   pip install pip-tools
#   pip-compile requirements.txt --output-file requirements-bundle.txt
#
# Install for bundling:
#   pip install -r requirements-bundle.txt
#
# Includes pyinstaller and all optional hub deps (pystray, pillow, pywinpty)
# so a single pip install is sufficient before running pyinstaller espai.spec.

# ── Runtime ──────────────────────────────────────────────────────────────────
fastapi>=0.136.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-core>=2.0.0
PyYAML>=6.0
aiofiles>=23.0.0
anyio>=4.0.0
sniffio>=1.3.0
starlette>=0.37.0
httptools>=0.6.0
python-multipart>=0.0.9
watchfiles>=0.21.0
websockets>=12.0
jsonschema>=4.22.0

# ── Optional: mDNS discovery ─────────────────────────────────────────────────
zeroconf>=0.131.0

# ── Optional: system tray icon ───────────────────────────────────────────────
pystray>=0.19.0
Pillow>=10.0.0

# ── Optional: PTY terminal (platform-specific) ───────────────────────────────
pywinpty>=2.0.0 ; sys_platform == "win32"
ptyprocess>=0.7.0 ; sys_platform != "win32"

# ── Optional: MQTT output ────────────────────────────────────────────────────
paho-mqtt>=1.6.0

# ── Optional: PlatformIO (ESP32 firmware toolchain) ──────────────────────────
platformio>=6.1.0

# ── Optional: OpenCV workers ─────────────────────────────────────────────────
# opencv-python>=4.8.0     # uncomment to include in bundle (large, ~50 MB)
# numpy>=1.24.0

# ── Packaging ────────────────────────────────────────────────────────────────
pyinstaller>=6.0.0
